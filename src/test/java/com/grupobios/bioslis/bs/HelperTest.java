package com.grupobios.bioslis.bs;

import java.time.LocalDate;
import java.util.Date;

import org.junit.jupiter.api.Test;

class HelperTest {
  /*
   * @Test void test() {
   * 
   * Date d = GregorianCalendar.getInstance().getTime();
   * System.out.println(DateFormaterHelper.dtimeToText(d)); String fechaCodificada
   * = "09/09/1999"; fechaCodificada = fechaCodificada.substring(0, 2) +
   * fechaCodificada.substring(3, 5) + fechaCodificada.substring(8, 10);
   * 
   * }
   * 
   * @Test void testPorcentajeEntero() {
   * 
   * PorcentajeAleatorio pa = new PorcentajeAleatorio();
   * System.out.println(pa.porcentajeDecimal());
   * System.out.println(pa.porcentajeEntero());
   * 
   * }
   * 
   * @Test void testConversionDecimal() { String NRO_REGEX =
   * "(([1-9]([0-9])*)|0)(,([0-9])+)"; Pattern p = Pattern.compile(NRO_REGEX);
   * Matcher m = p.matcher("10000000,03".replaceAll("\\.", "")); boolean b =
   * m.matches(); assert (b);
   * 
   * }

  @Test
  void rangeDate() {

    Date d = new Date(0L);

    LocalDate ldt = LocalDate.MAX;

    System.out.println(ldt);

    ldt = LocalDate.MIN;
    System.out.println(ldt);

    ldt = LocalDate.of(1961, 4, 16);
    System.out.println(ldt);

    System.out.println(d.toString());

    d = new Date(1961, 4, 1961);

    System.out.println(d.toString());

  }
   */
}
